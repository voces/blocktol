export const offsets = [
  [0, 0],
  [1, 0],
  [0, 1],
  [1, 1],
];

// Community Discord: the permanent invite code. Drives the join link
// (discord.gg/<code>) and the live "online now" lookup (see server's
// discordInfo route) — the invite endpoint needs no widget or bot token, just
// the code, so no extra Discord setup is required.
export const DISCORD_INVITE = "A6r7Ub6dRh";
